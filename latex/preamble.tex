\usepackage{ctex}

\usepackage[svgnames]{xcolor}

\usepackage{hyperref}
\hypersetup{
	bookmarksopen=true, % show pdf bookmarks
	bookmarksopenlevel=2, % 2-level bookmarks
	pdfstartview=FitH, % fit window width

	colorlinks, % color text of anchors instead of border
	linkcolor=DarkBlue,
	hyperindex, % hyperlinked indices
	unicode=true, % unicode~
	CJKbookmarks=true, % support CJK
}